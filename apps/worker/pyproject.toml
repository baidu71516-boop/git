[project]
name = "influencer-worker"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "backend-core",
  "celery[redis]>=5.4,<6",
]

[tool.uv]
package = false

[tool.uv.sources]
backend-core = { workspace = true }

